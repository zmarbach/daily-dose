package messengers

open class BaseMessenger {
    //common stuff needed for both text messenger and email messenger
    //if nothing, then delete this class

    //either make this abstract class or interface...
    //cuz will never need to initialize this class, only either TextMessenger or EmailMessenger
}